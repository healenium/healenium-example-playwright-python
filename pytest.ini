[pytest]
# https://playwright.dev/python/docs/test-runners
testpaths = tests
addopts = -v
# Markers for slow tests (deselect with '-m "not slow"')
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
